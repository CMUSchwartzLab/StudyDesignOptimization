conda create -n opt
conda activate opt
conda config --env --set subdir osx-64
conda install python=3.7
pip install smt
pip install msprime
conda install -c bioconda freebayes, delly
conda install -c bioconda freebayes delly
conda install -c bioconda minimap2 bwa bowtie2
pip install bioconda
pip install biopython
pip install datasketch
pip install psutil
pip install pandas 
pip install matplotlib
conda config --env --add channels defaults
conda config --env --add channels bioconda
conda config --env --add channels conda-forge
conda install samtools
conda update samtools
conda install bcftools

bwa index fakegenome.fa
samtools faidx fakegenome.fa
bowtie2-build fakegenome.fa fakegenome_index
